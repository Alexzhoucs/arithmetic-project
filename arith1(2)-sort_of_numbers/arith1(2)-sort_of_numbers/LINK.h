#include <stdio.h>
#include <iostream>
#include <time.h>
#include <math.h>

using namespace std;

double timetest(long);
int stringcmp(char a[], char b[]);
int randstr(long);
int mergesort(char a[], long b, long e);
